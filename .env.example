# このファイルはテンプレート（コミット対象）。
# 実際の .env は `make init` が生成する（git 管理外）。手で作るならこれをコピーして埋める。

# ターミナルからSSHするホストユーザー（通常は make init が id -un で自動設定）
HOST_USER=

# ホスト上の作業ディレクトリ（絶対パス）。コンテナにも同一パスでマウントされる。
PROJECT_DIR=

# code-server を公開するポート（前段の Caddy がこのポートへ reverse_proxy する）
PORT=8080
